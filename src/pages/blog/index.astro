---
export const prerender = false;

const acceptLang = Astro.request.headers.get('accept-language') || '';
const lang = acceptLang.toLowerCase().split(',').some(l => l.trim().startsWith('pl')) ? 'pl' : 'en';

return Astro.redirect(`/blog/${lang}/`, 302);
---
